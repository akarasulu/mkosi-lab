---
- name: Install btop on UKI lab VM
  hosts: all
  become: true

  tasks:
    - name: Update apt cache and install btop
      ansible.builtin.shell: |
        set -euo pipefail
        apt-get update
        DEBIAN_FRONTEND=noninteractive apt-get install -y btop
      args:
        executable: /bin/bash
