# {{ mkosi_esp_project_name }}

Generated mkosi ESP project.

- Distribution: {{ mkosi_esp_distribution }}
- Release: {{ mkosi_esp_effective_release | default(mkosi_esp_release) }}
- Architecture: {{ mkosi_esp_architecture }}
{% if mkosi_esp_effective_architecture | default(mkosi_esp_architecture) != mkosi_esp_architecture %}
- mkosi compatibility architecture: {{ mkosi_esp_effective_architecture }}
{% endif %}
- Output: {{ mkosi_esp_output_format }}
{% if mkosi_esp_effective_output_format | default(mkosi_esp_output_format) != mkosi_esp_output_format %}
- mkosi compatibility output: {{ mkosi_esp_effective_output_format }}
{% endif %}

The build output is intentionally ignored by Git. Source inputs such as
`mkosi.conf` and `mkosi.extra/` are tracked for provenance.
